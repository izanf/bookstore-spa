const fontSizes = {
  xs: '0.75rem',
  sm: '1rem',
  md: '1.25rem',
  lg: '1.5rem',
  xl: '2rem',
  xlx: '3rem',
  xlxx: '4rem',
}

const colors = {
  black: '#000',
  white: '#fff',
  grayLight: '#DDD',
}

const theme = {
  fontSizes,
  colors,
}

export default theme;
